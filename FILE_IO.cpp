#include <stdio.h>
#include <string.h>
#include <assert.h>
#include <vector>
#include <algorithm>
#include <iostream>
#include <stdlib.h>
#include <set>

/*
ID: iamnotnad1
LANG: C++
TASK: palsquare
*/

   using namespace std;

   int main(void)
   {
      FILE *fin, *fout, *dict;
   
      fin = fopen("palsquare.in", "r");
      fout = fopen("palsquare.out", "w");
      assert(fin != NULL && fout != NULL);
      
      //fscanf(fin, "%s", num_string);
    
      //fprintf(fout, "%s\n", valid_names[i]);
   	
      return 0;
   }
